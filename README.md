# Oauth2.0_login_flow
A minimal web app that lets a user log in with GitHub (using OAuth 2.0), and displays their GitHub username and email after login — proving you understand the full authorization code flow end to end.
